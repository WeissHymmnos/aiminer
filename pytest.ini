[pytest]
testpaths = tests/unit tests/integration tests/external
markers =
    external: requires external credentials, network access, or paid APIs
    integration: exercises multiple subsystems or filesystem-backed components
addopts = -m "not external"
